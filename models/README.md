# Model Data

- `seed-data.json` — Source of truth for pricing & benchmark scores (53 models)
- `seed-models.ts` — Script to import into SQLite: `npm run seed:models`
- `dashboard.html` — Model comparison table (served at `/models/dashboard`)

See [docs/model-pricing.md](../docs/model-pricing.md) for the full pricing reference.